import 'package:flutter/material.dart';

class UiConstant {
  static const double pagePaddingSize = 18;
  static const EdgeInsets horizontalPadding =
      EdgeInsets.symmetric(horizontal: pagePaddingSize);

  static const EdgeInsets verticalPadding =
      EdgeInsets.symmetric(vertical: pagePaddingSize);

  static const EdgeInsets alllPadding = EdgeInsets.all(pagePaddingSize);
}
